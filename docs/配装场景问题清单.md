# doubaoqueen

注意一下，此处因为我一瞬间接收到的信息太多，可能词不汇意，你遇到任何不清楚的必须向我提问！！此处有一些我认为的逻辑问题，所以暂不写代码执行，先拟草

1，点"制式套装"卡片后，就会进入一些推荐的预设装备，比较重要的一点，这一点看样子还不是简单的实现，我简单说一下流程：
1.1点击”制式套装“，进入装备预设界面，
ps:装备预设界面详情----模板是：
a.顶部有推荐的当前的游戏能力程度的玩家自认实力和需求的装配方向”新兵“，”标准“，”精锐“，”特种“，”定制“。目前不清楚是否为艺术字。鼠标悬停在每个标签上时，会改变颜色的样式----绿色；
b.我个人认为不好处理的一点就是：预设配装是有限制的。就在推荐的三套装配（“均衡”，”生存“，”火力“）方案的右上角，有预设的上限标识，是五个图片UI：[拥有：🎴10/10   🎴3/5    🎴1/5    🎴0/5]。顶部有五种玩家能力自认，但卡片类型只有4种，当然，我预测“定制”应该是扣除价值最接近的。这个逻辑不算特别难，但是我认为是得标记一下几张卡片的。
c.提了一嘴的预设配装类型，组件占了界面的大头，但是识别的话很容易应该。每个预设配装组件顶部都是有“均衡”，”生存“，”火力“的字体的，麻烦的是需要确定消耗什么类型的卡片。
1.2在预设配装的底部，点击卡片以消耗来一键配装：
a.若符合地图、等级、模式等规则，则直接成功
b.若不符合，在预设配装主题的顶部会出现提示，此处我认为算是配装的第二个难点，需要合适的屏幕识别时机，否则可能错过
c.有点没印象了，明天上号游戏再看看吧，此处有一个小机制的，你预留一到两个位置吧
1.3若成功完成点击预设装配，回到配装界面，底部依旧会有提示弹框。若点击x，则取消当前配装



## 订正 1.2.c

c. 对应的模式的”制式套装“的预设，顶部”新兵（[卡片数量]）“，”标准（[卡片数量]）“，”精锐（[卡片数量]）“，”特种（[卡片数量]）“，”定制（[卡片数量]）“ 中的（[卡片数量]）会变为绿色，这是预设装配的机制吧
d. 4种卡片对应名称为：”新兵制式装备券“，”标准制式装备券“，”精锐制式装备券“，”特种制式装备券“

# DeepSeek

收到，这轮**只拟草、不写码**。我先把你的描述翻译成"状态机草稿"，再把所有没吃准的地方列成问题——你明天上号时按问题看一遍就行。

## 草稿：制式套装配装链路（v0，待你的答案修订）

```
配装界面 (loadout)
  └─ 点击「制式套装」卡片 (模板已有 ✅)
        ↓
装备预设界面 (新界面，待标定) ←—— 需要新增界面 + 标记
  ├─ 顶部 5 个方向标签：新兵/标准/精锐/特种/定制
  │    （疑似美术字？悬停变绿）
  ├─ 3 张预设卡：均衡/生存/火力（卡名可 OCR）
  ├─ 右上角额度标识：🎴 拥有 x/y × N 张（图标模板待裁）
  │
  ├─ 默认动作：点「新兵」标签 + 点「均衡」卡（待你确认选哪张）
  │
  ├─ 点击预设卡 →
  │   ├─ ✅ 成功 → 自动回配装界面 + 底部弹框（不点 X）→ 点「确认配装」→（入局提醒）→ 出发
  │   └─ ❌ 失败（不符合规则）→ 顶部弹提示 → 预留分支：识别失败 → 人工确认/换卡重试
  │
  └─ 【预留位 1】【预留位 2】（1.2c 那个"小机制"，明天补）
```

**关于"提示容易错过"（难点 2）的初步对策**：不追着瞬态弹窗跑，而是**用状态迁移判断成败**——点完预设卡后立刻连续识别：2 秒内回到配装界面 = 成功；还在预设界面 = 失败，再去顶部 OCR 找提示词。这样就算提示一闪而过，我们也知道"没成功"，不会漏判。

## 必须向你提问的（明天上号时照着看）

1. **额度标识到底是几张图**？你写"五个图片UI"但列了 4 个（10/10、3/5、1/5、0/5）——是 4 张还是 5 张？每张卡代表什么（不同档位的"预设券"？）
2. **顶部 5 个标签**（新兵/标准/精锐/特种/定制）：是点一下**切换**推荐列表，还是只做筛选？我们自动化默认点「新兵」对吗？标签文字是普通方块字还是美术字（肉眼能分辨吗）？
3. **悬停变绿**：是"鼠标一上去变绿、移开恢复"，还是"选中后**保持**绿色"？
4. **三张预设卡选哪张跑刀**？（我猜「均衡」）点卡片是"一点立即消耗额度并配装"，还是点完还有一步确认按钮？
5. **成功后的底部弹框**：不点 X 它会自己消失吗？它会不会**挡住「确认配装」按钮**？我们的计划是"不点 X，直接点确认配装"——可行吗？
6. **失败提示**（顶部）：大概停几秒？记得文字吗（比如"等级不足/地图不符"）？
7. **「定制」标签**你说的"扣除价值最接近的卡片"——是指定制预设会消耗最接近价格的额度卡吗？
8. **预留位**：除了 1.2c 的小机制，你说的"预留一到两个位置"具体指什么环节？我的理解是"失败重试策略"和"成功弹框处理"两处，对吗？

## 明天顺便截的图（用 `dotnet run -- capture 三角洲行动`）

① 预设界面全景（点开制式套装后）；② 鼠标悬停在标签上的绿色态；③ 额度区特写（右上角那排 🎴）；④ 失败提示出现的瞬间（如果能抓到）；⑤ 成功后回配装界面的底部弹框。

你的答案回来后，我先把终稿写进 `docs/配装设计.md`，给你过目后再动代码。

# 关于你的提问的回答

1.额度标识是4张图。是这样的之前有些版本是有送定制券的，但是现在取消了，所以现在只有四种”制式装备券“，对应的话我在上面的订正中回应了，官方做的名称很容易判断，你可以看看

2.1是点一下切换列表

2.2选择了“零号大坝”，常规模式，默认就是”新兵“

2.3我的肉眼能判断

3.鼠标移上去，方块底部变绿，点击选中之后整体边框变绿

4.点击下方消耗‘制式装备券’处，就会直接回到配装界面，没有再一次确认的按钮。在点击配装界面中的”确认配装“界面之后，才会消耗一张对应的'制式装备券'

5.1不点x不会自动消失，点x后取消掉”制式套装“中预设的装备。

5.2不会挡住「确认配装」按钮

5.3直接点「确认配装」按钮我认为可以

6.从出现到结束，大概一点五秒；文字为：“xx制式装备券 无法进入零号大坝-常规”；‘xx制式装备券’ 字体为黄色，”无法进入零号大坝-常规“是白色，肉眼能分辨

7.这个问题，回答1是有的

8.差不多这个意思吧，可能是我记错了。订正中我是回应了的，记忆有点错乱了。。。哈哈哈
